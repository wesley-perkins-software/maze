---
import CategoryLayout from '../layouts/CategoryLayout.astro';
import LibraryMazeCard from '../components/ui/LibraryMazeCard.astro';
import { LibraryProgress } from '../components/library/LibraryProgress';
import { getCategoryBySlug } from '../types/maze.js';
import { getLibraryMazesByDifficulty } from '../lib/library/catalog.js';

const category = getCategoryBySlug('expert-mazes')!;
const mazes = getLibraryMazesByDifficulty('expert');
const allIds = mazes.map((m) => m.id);

const BASE = 'https://mazethis.com';
const pageUrl = `${BASE}/expert-mazes`;

const faqItems = [
  {
    q: 'What are Expert Mazes?',
    a: 'Expert Mazes are puzzle games on an 80×80 grid featuring long corridors and deep branching. They are a serious challenge designed for experienced maze solvers who have worked through the Large collection and are ready for a significant step up.',
  },
  {
    q: 'How many Expert Mazes are there?',
    a: 'There are 25 Expert Mazes in the collection, numbered Expert #001 through Expert #025. Every maze has a fixed layout — the same puzzle is always at the same URL.',
  },
  {
    q: 'Are Expert Mazes free to play?',
    a: 'Yes. Every Expert Maze is free to play directly in your browser — no account, no download, and no sign-up required.',
  },
  {
    q: 'How do I track my progress in Expert Mazes?',
    a: "Progress is saved on your device. After solving a maze, a green checkmark badge appears on the card and the progress bar updates. Your completions are stored in your browser's local storage, so they persist between visits without requiring an account.",
  },
  {
    q: 'What makes Expert Mazes harder than Large?',
    a: 'The 80×80 grid is 78% larger than the Large 60×60 grid, producing proportionally longer solution paths and more complex branching. Expert Mazes also feature deeper corridor networks that make backtracking more time-consuming. Expect a significant jump from Large in both solve time and difficulty.',
  },
];

const schemaLD = JSON.stringify({
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'WebPage',
      '@id': pageUrl,
      url: pageUrl,
      name: category.title,
      description: category.description,
      isPartOf: { '@id': `${BASE}/#website` },
      breadcrumb: { '@id': `${pageUrl}#breadcrumb` },
    },
    {
      '@type': 'BreadcrumbList',
      '@id': `${pageUrl}#breadcrumb`,
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Home', item: `${BASE}/` },
        { '@type': 'ListItem', position: 2, name: 'Maze Library', item: `${BASE}/maze-library` },
        { '@type': 'ListItem', position: 3, name: 'Expert Mazes' },
      ],
    },
    {
      '@type': 'FAQPage',
      mainEntity: faqItems.map(({ q, a }) => ({
        '@type': 'Question',
        name: q,
        acceptedAnswer: { '@type': 'Answer', text: a },
      })),
    },
    {
      '@type': 'ItemList',
      name: 'Expert Mazes',
      itemListElement: mazes.map((m, i) => ({
        '@type': 'ListItem',
        position: i + 1,
        url: `${BASE}/play/library/${m.id}`,
      })),
    },
  ],
});
---

<CategoryLayout
  category={category}
  totalCount={mazes.length}
  schemaLD={schemaLD}
  tagline="25 free expert maze puzzles on an 80×80 grid. Long corridors and deep branching — a serious challenge for experienced solvers."
>
  <!-- Progress row -->
  <div class="mb-6 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
    <div class="min-w-0 flex-1 max-w-xs">
      <LibraryProgress client:load total={mazes.length} difficulty={category.difficulty} />
    </div>
    <a
      id="play-next-link"
      href={`/play/library/${allIds[0]}`}
      data-all-ids={JSON.stringify(allIds)}
      class="btn-primary text-sm px-4 py-2 shrink-0 self-start sm:self-auto"
    >
      Play Next
    </a>
  </div>

  <!-- Card grid -->
  <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4">
    {mazes.map((entry, i) => (
      <LibraryMazeCard entry={entry} index={i} />
    ))}
  </div>

  <!-- FAQ -->
  <div class="mt-12 max-w-2xl">
    <h2 class="text-xl font-bold mb-5" style="color: var(--color-charcoal);">About Expert Mazes</h2>
    <dl class="space-y-5">
      {faqItems.map(({ q, a }) => (
        <div>
          <dt class="font-semibold text-slate-800">{q}</dt>
          <dd class="mt-1 text-slate-600 text-sm">{a}</dd>
        </div>
      ))}
    </dl>
  </div>
</CategoryLayout>

<script>
  (function () {
    try {
      var raw = localStorage.getItem('mazethis.library.progress');
      var ids = raw ? JSON.parse(raw)?.completedMazeIds : [];
      if (!Array.isArray(ids)) return;
      var done = new Set(ids);

      document.querySelectorAll('[data-maze-id]').forEach(function (el) {
        if (done.has(el.getAttribute('data-maze-id'))) {
          el.classList.add('maze-card-completed');
        }
      });

      var link = document.getElementById('play-next-link');
      if (!link) return;
      var allIds = JSON.parse(link.getAttribute('data-all-ids') ?? '[]');
      var next = allIds.find(function (id) { return !done.has(id); });
      if (next) {
        link.setAttribute('href', '/play/library/' + next);
      } else {
        link.textContent = 'All Complete ✓';
        link.removeAttribute('href');
        link.style.opacity = '0.55';
        link.style.pointerEvents = 'none';
      }
    } catch (_) {}
  })();
</script>
